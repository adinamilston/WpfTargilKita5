#include <iostream>
using namespace std;

int main()
{
	int a, b;
	
	cout << " enter two numbers: one negative and one positive\n";
	cin >> a >> b;
	int x;
	//x = a + b;
	//if (x) cout << " true\n" else cout << "false\n";
	if (x = a + b)
		// they are not same distance from y axis
		cout << " difference is not zero: " << x << endl;
	else
		cout << "difference is zero\n";
	return 0;

}